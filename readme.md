
---

# **LibRAGrary — твой локальный книжный червь 🪱**

**LibRAGrary** — это полностью локальная RAG-система для работы с вашей личной библиотекой.
Проект позволяет развернуть LLM, эмбеддер и ранкер целиком офлайн, используя **OpenVINO GenAI**.
Просто положите свои книги в папку и общайтесь с ними как с библиотекарем.

---

## Возможности

* Полностью локальная RAG система
* Поддержка книг в формате `.txt`
* Оптимизация моделей через **OpenVINO GenAI**
* Создание индекса Faiss для векторного поиска

---

## Используемые модели

* Модель LLM: **[OpenVino/Mistral-7B-Instruct-v0.3-int4-cw-ov](https://huggingface.co/OpenVINO/Mistral-7B-Instruct-v0.3-int4-cw-ov)**
* Модель эмбеддера: **[intfloat/multilingual-e5-large](https://huggingface.co/intfloat/multilingual-e5-large)** (optimum-cli-converted to OpenVino IR)
* Модель ранкера: **[BAAI/bge-reranker-v2-m3](https://huggingface.co/BAAI/bge-reranker-v2-m3)** (optimum-cli-converted to OpenVino IR)

Все модели разворачиваются через OpenVINO GenAI. Работа проекта была проверена на Intel Core 5 Ultra 125h + 16gb RAM

---

## Установка

### 1. Подготовьте окружение

```bash
python -m venv venv
venv\Scripts\activate         
pip install -r requirements.txt
```

### 2. Установите модели OpenVINO GenAI

Если вы хотите использовать те же модели, что и при тестах, запустите скрипт загрузки LLM и загрузите эмбеддер и ранкер с помощью optimum cli

```bash
python download_models.py
optimum-cli export openvino --model BAAI/bge-reranker-v2-m3 --weight-format fp32 models/Rankers/BAAI_bge-reranker-v2-m3
optimum-cli export openvino --model intfloat/multilingual-e5-large --weight-format fp32 models/Embedders/intfloat_multilingual-e5-large
```

### 3. Добавьте свои книги

Скопируйте текстовые файлы (`.txt`) в директорию:

```
books/
```

Например:

```
books/
 ├── Frank Herbert_dune.txt
 ├── Leo Tolstoy_war and peace.txt
 └── Isaac Asimov_foundation.txt
```
Разделите имя автора и название книги с помощью `_`. Таким образом, вы добавите имя автора и название книги в метаданные чанков, которые смогут быть использованы в дальнейшем.
---

## Запуск

После подготовки просто запустите основной скрипт:

```bash
python main.py
```

Ваша локальная RAG-система готова.
Теперь вы можете задавать вопросы по вашим книгам, искать цитаты и контекст, анализировать персонажей и многое другое.

## Обратите внимание! 
Каждый первый запуск проекта может занимать продолжительное время в связи с разверткой моделей.

---

## TODO

- [ ] добавить поддержку других форматов книг (PDF, EPUB, FB2)
- [ ] добавить интерфейс
- [ ] подключить новые модели
- [ ] улучшить ранжирование и поиск
- [ ] добавить обработку текстов книг с добавлением метаданных
- [ ] добавить обработку запросов с указанием на авторство или название книги
- [ ] добавить возможность удаления книг из библиотеки и индекса
---

## Лицензия

MIT-licensed